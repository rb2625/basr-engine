# BASR 2.0 - new ingestion pipeline (basr/orchestrator.py)
httpx
websockets
groq
supabase
fpdf2
python-dotenv
statsmodels   # Phase 4 anomaly detection (STL decomposition)

# v1 pipeline only (frozen until Phase 6 cleanup): praw, playwright are no
# longer used by the new pipeline and are intentionally NOT installed.
arabizikit   # Arabizi -> Arabic transliteration (Gulf dialect, learned layer)
